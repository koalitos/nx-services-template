apiVersion: apps/v1
kind: Deployment
metadata:
  name: template-supa-auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: template-supa-auth
  template:
    metadata:
      labels:
        app: template-supa-auth
    spec:
      containers:
        - name: auth
          image: ghcr.io/YOUR-ORG/template-supa-auth:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  key: AUTH_PORT
                  name: template-supa-config
            - name: SUPABASE_REALTIME_CHANNEL
              valueFrom:
                configMapKeyRef:
                  key: SUPABASE_REALTIME_CHANNEL
                  name: template-supa-config
          envFrom:
            - secretRef:
                name: template-supa-secrets
          readinessProbe:
            httpGet:
              path: /auth/health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /auth/health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: template-supa-auth
spec:
  selector:
    app: template-supa-auth
  ports:
    - name: http
      port: 80
      targetPort: 3001
  type: ClusterIP
